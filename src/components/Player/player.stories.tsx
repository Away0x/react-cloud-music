import React from 'react';

import Player from '.';

export default {
  title: 'Components/Player',
  component: Player
};

export const Default = () => {
  return <Player>Player component</Player>;
};
